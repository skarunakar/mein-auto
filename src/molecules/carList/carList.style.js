import { makeStyles } from '@material-ui/core';

const useStyle = makeStyles({
    root: {
        marginTop: '2.4rem',
    },
    gridItem: {
        padding: '0 0.8rem',
    },
    listHeader: {
        fontSize: '1.8rem',
        fontFamily: 'Roboto',
        fontWeight: 700,
        marginBottom: '1.2rem'
    },
    paginationContainer: {
        margin: '0.8rem',
    }

})

export default useStyle;